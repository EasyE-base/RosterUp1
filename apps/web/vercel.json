{
  "buildCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.17.1 --activate && pnpm install --no-frozen-lockfile && pnpm --filter=web build",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.17.1 --activate && pnpm install --filter=web --prod=false",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
