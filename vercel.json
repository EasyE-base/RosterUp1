{
  "buildCommand": "npm install -g pnpm@10.17.1 && pnpm install --no-frozen-lockfile && pnpm --filter=web build",
  "installCommand": "echo Skipping default install",
  "outputDirectory": "apps/web/.next",
  "framework": null
}
